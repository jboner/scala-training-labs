package scalatraining.lab2

object Lab2 {
  def firstElementInList[T](l: List[T]): T = { error("fix me") }

  def lastElementInList[T](l: List[T]): T = { error("fix me") }

  def concatLists(list1: List[String], list2: List[String]): List[String] = { error("fix me") }

  def sortList(list: List[String]):List[String] = { error("fix me") }
  
  def elementExists(theList: List[String], elementToSearchFor: String): Boolean = { error("fix me") }

  def oddElements(listOfInts: List[Int]): List[Int] = { error("fix me") }
}


